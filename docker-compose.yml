services:
  web:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    environment:
      PORT: 3000
      APP_BASE_URL: http://localhost:3000
    volumes:
      - ./data:/app/data
    init: true
